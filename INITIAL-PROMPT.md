# Initial Claude Code prompt

Paste this into a fresh Claude Code session in the empty repo (after copying `CLAUDE.md`, `README.md`, and `docs/` into place).

---

Working in a fresh repo. This is "Bramble," an open-source baby name swipe app for couples.

`CLAUDE.md`, `README.md`, and three docs under `docs/` describe the project, architecture, and Phase 0 scope. Read all of them before doing anything else.

Task: execute `docs/PHASE-0.md`. Phase 0 only — Phase 1+ is explicitly out of scope.

Workflow:
1. Read the docs.
2. Produce a short plan (5–10 bullets) covering scaffold, dataset, KV schema, swipe UI, match view, deploy. Flag any choices you're making that aren't explicit in the docs.
3. Stop and wait for my approval before writing code.

Conventions are in `CLAUDE.md`. Hold to them — especially the "no new runtime deps without flagging" and "stop and ask if a decision isn't covered" rules.
